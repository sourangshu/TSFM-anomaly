# mTSBench baseline detectors, pinned to the versions actually installed and
# verified in the conda env `mtsbench` (python 3.11.14) on 2026-07-23.
#
# This is upstream mTSBench/requirements.txt + numba, with the resolved versions
# pinned. Upstream leaves pandas/scikit-learn/etc. unbounded, so an unpinned
# install today will not reproduce this environment tomorrow — notably `pandas`
# resolved to 3.0.5 here, which is a major version ahead of what the detector
# code was written against. It works (verified end-to-end on PCA/SMD), but it is
# exactly the kind of thing that should be pinned rather than left to chance.
#
#   conda create -n mtsbench python=3.11 -y
#   ~/miniconda3/envs/mtsbench/bin/pip install -r requirements.txt
#
# torch 2.3.0 from PyPI ships bundled CUDA 12.1 runtime libs; no separate CUDA
# toolkit install is needed. See "Does this need CUDA?" in README.md.

# ── core numerics ────────────────────────────────────────────────────────────
numpy==1.26.4          # must stay <2.0 — upstream pins this and numba 0.66 agrees
pandas==3.0.5
scipy==1.17.1
scikit-learn==1.9.0
numba==0.66.0          # HBOS only (@njit); not in upstream requirements.txt

# ── deep detectors (CNN, LSTMAD, USAD, AutoEncoder, OmniAnomaly, TranAD,
#    Donut, OFA, ALLM4TS) ──────────────────────────────────────────────────────
torch==2.3.0
torchinfo==1.8.0       # model summaries in CNN/LSTMAD/OFA/Donut
tqdm==4.69.0

# ── OFA only: builds transformers.GPT2Model ('gpt2' downloads on first use) ───
transformers==4.53.3
huggingface-hub==0.30.2
einops==0.8.2

# ── upstream deps, required only by detectors OUTSIDE the config.py set
#    (SAND/Series2Graph/MatrixProfile/NORMA etc.). Kept so this file reproduces
#    the verified environment rather than a narrower one that was never tested. ─
arch==8.0.0
Cython==3.2.8
h5py==3.16.0
hurst==0.0.5
matplotlib==3.11.1
statsmodels==0.14.6
stumpy==1.14.1
tslearn==0.9.0
