# Carved window pickles + the symlinked fold views (7.5 GB). Rebuild with:
#   bash run_prepare_family.sh
pool/per_dataset/
run/prepared/
per_family/

# Checkpoints and scored outputs — regenerated by run_study.sh
run/ckpt/
*/ckpt/
results/

# pool/manifest.json IS tracked on purpose: it records the exact file lists, window
# counts and anomaly counts each dataset contributed, which is the provenance for
# every number in the study.
